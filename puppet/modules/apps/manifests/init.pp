class apps {
  package{'google-chrome':
    provider => homebrew
  }

  package{'spotify':
    provider => homebrew
  }

  package{'slack':
    provider => homebrew
  }

  package{'flycut':
    provider => homebrew
  }

  package{'discord':
    provider => homebrew
  }

  package{'wakatime-cli':
    provider => homebrew
  }

  package{'bettertouchtool':
    provider => homebrew
  }
}
